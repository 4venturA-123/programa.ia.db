class AIModel {
    constructor() {
        // Inicializar el modelo
    }

    processInput(input, context) {
        // Procesar la entrada y devolver una respuesta basada en el contexto
        return `Pregunta: ${input}\nRespuesta basada en el contexto: ${context}`;
    }
}

module.exports = AIModel;
